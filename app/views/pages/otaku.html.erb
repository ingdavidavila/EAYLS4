<h1 class="text-center"> Por Favor Bañate </h1>
<div class="flickr_pagination">
    <%= will_paginate @articulo, :container => false %>
</div>

<%= render 'articulos/articulos' %>

<div class="flickr_pagination mb-4">
    <%= will_paginate @articulo, :container => false %>
</div>
